# This project uses yt-dlp via command line interface
# Install with: pipx install yt-dlp
# FFmpeg is also required: brew install ffmpeg

# Python dependencies for new platform downloaders
requests>=2.31.0 